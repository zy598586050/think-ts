{
  "name": "think-ts",
  "version": "1.0.0",
  "description": "This is a node framework based on koa2 encapsulation.",
  "scripts": {
    "build": "npx tsc",
    "dev": "nodemon --exec ts-node index.ts",
    "view": "browser-sync start --proxy 'http://localhost:5985' --files 'views/*.vue' --port 3000",
    "start": "pm2 start dist/config/env.js",
    "start:test": "NODE_ENV=test pm2 start dist/config/env.js"
  },
  "author": "zhangyu",
  "license": "ISC",
  "dependencies": {
    "@types/think-ts-lib": "file:../think-ts-type",
    "nodemon": "^3.0.1",
    "think-ts-lib": "file:../think-ts-lib",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
